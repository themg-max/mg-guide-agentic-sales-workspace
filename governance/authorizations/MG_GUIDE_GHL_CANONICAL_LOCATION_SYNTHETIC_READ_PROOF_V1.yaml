# Human-authorized grant — live execution NOT performed by the binding unit.
# Exact synthetic record IDs remain in the private control plane only.
grant_id: MG_GUIDE_GHL_CANONICAL_LOCATION_SYNTHETIC_READ_PROOF_V1
status: AUTHORIZED_FOR_EXECUTION
execution_status: AUTHORIZED_NOT_EXECUTED
proposed_at_utc: "2026-08-11T22:45:00Z"
authorized_at_utc: "2026-08-12T02:02:01Z"
workflow: meeting_follow_up_v1

environment: canonical GHL location

environment_classification_note: >
  This grant does NOT classify the production location as a test environment.
  Only explicitly designated synthetic records, bound via a private allowlist
  of exact record IDs, may be read.

human_activation:
  decision: AUTHORIZED_FOR_EXECUTION
  human_approver: Aaron Chandler
  human_approval_role: repository maintainer / CRM operator
  human_approved_at_utc: "2026-08-12T02:02:01Z"
  human_signature: APPROVED
  public_disclosure_of_exact_ids: false
  token_value_recording: false

activation_satisfied:
  - one explicitly designated synthetic contact (private allowlist)
  - one explicitly designated synthetic opportunity (private allowlist)
  - private allowlist of exact record IDs (never written to this public repo)
  - relationship verified between bound synthetic contact and opportunity
  - secret path already authorized (no IAM or secret mutation)
  - PIT canonical location verified by human (token value not recorded)
  - explicit human authorization recorded for this grant id

activation_assertions:
  SYNTHETIC_CONTACT_BOUND: "YES"
  SYNTHETIC_OPPORTUNITY_BOUND: "YES"
  RELATIONSHIP_VERIFIED: "YES"
  PRIVATE_ALLOWLIST_COMPLETE: "YES"
  PIT_CANONICAL_LOCATION_VERIFIED: "YES"
  IAM_CHANGE_REQUIRED: "NO"
  SECRET_EXISTS: "YES"
  SECRET_ACCESS_ALREADY_PROVISIONED: "YES"

allowed_live_operations:
  - get-contact (exact allowlisted synthetic ID only; MAX=1)
  - get-opportunity (exact allowlisted synthetic ID only; MAX=1)
  - get-pipelines (pipeline metadata only)

forbidden:
  - search-contacts-advanced
  - search-opportunity
  - search-opportunities-advanced
  - broad contact search
  - broad opportunity search
  - create-note
  - update-opportunity
  - update-opportunity-status
  - all CRM writes
  - email/SMS
  - raw REST
  - real customer/contact reads
  - any non-allowlisted record ID

budgets:
  CONTACT_RECORD_READ_BUDGET: 1
  OPPORTUNITY_RECORD_READ_BUDGET: 1
  GHL_WRITES_ALLOWED: 0
  REAL_CUSTOMER_RECORD_READS_ALLOWED: 0

authority_limits:
  AUTHORITY_EXPANSION: "NO"
  DEPLOYMENT_AUTHORIZED: "NO"
  GEMINI_ADK_AUTHORIZED: "NO"

proof_requirements:
  - redacted output only
  - synthetic data only
  - GHL_WRITES: 0
  - REAL_PRODUCTION_RECORD_READS: 0
  - no record IDs written to public repo

public_binding_artifacts:
  - proof/canonical-synthetic-read-binding-v1/synthetic-record-binding.yaml
  - proof/canonical-synthetic-read-binding-v1/authorization.md
  - proof/canonical-synthetic-read-binding-v1/execution-manifest.yaml
  - proof/canonical-synthetic-read-binding-v1/return-packet.md
  - proof/canonical-synthetic-read-binding-v1/proof-return.yaml

notes:
  - Binding unit records authorization only; GHL_LIVE_CALLS=0 for the binding PR.
  - Live reads require a separate bounded execution unit under this grant.
